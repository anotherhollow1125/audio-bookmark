import React from "react";
import ReactDOM from "react-dom/client";
import "@/App.css";
import ConfigElm from "./ConfigElm";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <ConfigElm />
  </React.StrictMode>
);
